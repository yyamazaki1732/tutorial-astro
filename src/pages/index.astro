---
import { tuple } from "astro/zod";
const isProduction = import.meta.env.NODE_ENV === "production" ? true : false;
---

{
  isProduction == true ? (
    <meta http-equiv="refresh" content="0; url=/test/en/" />
  ) : (
    <meta http-equiv="refresh" content="0; url=/en/" />
  )
}
